# ✅ إعداد FCM للـ APK Release - مكتمل

## ✅ الخطوات المكتملة

### 1. ✅ SHA Fingerprints تم إضافتها في Firebase Console

**SHA-1:**
```
fd:94:93:92:a4:3b:77:7a:66:cf:6b:2a:31:cd:1b:63:27:8a:82:cd
```
✅ **كامل وصحيح!**

**SHA-256:**
```
da:79:d0:59:45:c0:2a:3c:dc:58:dd:42:49:4e:ef:ec:86:65:9e:cd:67:fa:1a:35:e6:23:82:d4:79:99:3a:80
```
✅ **كامل وصحيح!**

### 2. ✅ google-services.json تم تحديثه
تم استبدال ملف `android/app/google-services.json` بالملف الجديد من Firebase Console.

### 3. ✅ Package Name متطابق
- Firebase Console: `com.munqeth.app` ✅
- google-services.json: `com.munqeth.app` ✅
- build.gradle: `com.munqeth.app` ✅

---

## 🚀 الخطوات التالية - بناء APK Release

الآن يجب بناء APK جديد ليستخدم الإعدادات المحدثة:

### 1. تنظيف البناء:
```bash
cd munqeth
flutter clean
flutter pub get
```

### 2. بناء APK Release:
```bash
flutter build apk --release
```

أو لبناء App Bundle (لنشره في Google Play):
```bash
flutter build appbundle --release
```

### 3. تثبيت APK على الجهاز:
```bash
# احذف التطبيق القديم أولاً (إن كان مثبتاً)
adb uninstall com.munqeth.app

# ثبت APK الجديد
adb install build/app/outputs/flutter-apk/app-release.apk
```

أو ثبت APK يدوياً من المسار:
```
munqeth/build/app/outputs/flutter-apk/app-release.apk
```

---

## 🧪 اختبار الإشعارات

بعد تثبيت APK الجديد:

1. **افتح التطبيق**
2. **سجل الدخول**
3. **أنشئ طلب** (تاكسي، توصيل، إلخ)
4. **تحقق من وصول الإشعارات** ✅

### ما يجب أن يحدث:
- ✅ يجب أن تصل الإشعارات للمستخدم
- ✅ يجب أن تصل الإشعارات للسائقين
- ✅ لا يجب أن تظهر أخطاء `messaging/registration-token-not-registered`

---

## 📋 ملخص الإعداد

| العنصر | الحالة | القيمة |
|--------|--------|--------|
| **SHA-1** | ✅ مكتمل | `fd:94:93:92:a4:3b:77:7a:66:cf:6b:2a:31:cd:1b:63:27:8a:82:cd` |
| **SHA-256** | ✅ مكتمل | `da:79:d0:59:45:c0:2a:3c:dc:58:dd:42:49:4e:ef:ec:86:65:9e:cd:67:fa:1a:35:e6:23:82:d4:79:99:3a:80` |
| **Package Name** | ✅ متطابق | `com.munqeth.app` |
| **google-services.json** | ✅ محدث | تم استبداله بالملف الجديد |
| **Firebase Project** | ✅ صحيح | `munqethnof` |

---

## 🔧 تحسينات إضافية تم تطبيقها

### 1. ✅ Backend - حذف FCM Tokens غير الصالحة تلقائياً
تم تحسين `backend/utils/notificationService.js` لحذف FCM tokens غير الصالحة من قاعدة البيانات تلقائياً عند فشل الإشعارات.

### 2. ✅ معالجة أفضل للأخطاء
الـ backend الآن يتعامل مع أخطاء `messaging/registration-token-not-registered` بشكل أفضل ويحذف الـ tokens غير الصالحة تلقائياً.

---

## ⚠️ ملاحظات مهمة

1. **احذف التطبيق القديم:**
   قبل تثبيت APK الجديد، احذف التطبيق القديم بالكامل من الجهاز.

2. **سجل الدخول مرة أخرى:**
   بعد تثبيت APK الجديد، سجل الدخول مرة أخرى لضمان الحصول على FCM token جديد.

3. **انتظر قليلاً:**
   قد يستغرق Firebase بضع دقائق لتطبيق التغييرات بعد إضافة SHA fingerprints.

4. **Google Play App Signing:**
   إذا كنت تستخدم Google Play App Signing (عند النشر في Google Play Store):
   - قد تحتاج إلى إضافة SHA-256 من Google Play Console أيضاً
   - Google Play Console → Your app → Release → Setup → App signing
   - انسخ SHA-256 certificate fingerprint وأضفه في Firebase Console

---

## 🐛 إذا استمرت المشكلة

### 1. تحقق من Logcat:
```bash
adb logcat | grep -i firebase
```

ابحث عن:
- ✅ "FirebaseApp initialization successful"
- ✅ "FCM Token obtained successfully"
- ❌ أي أخطاء Firebase

### 2. تحقق من FCM Token:
في التطبيق، تأكد من أن FCM token يتم الحصول عليه وإرساله للسيرفر عند تسجيل الدخول.

### 3. تحقق من Firebase Console:
- تأكد من أن SHA-1 و SHA-256 موجودان في Firebase Console
- تأكد من تطابق package name

### 4. نظف البناء وأعد البناء:
```bash
flutter clean
cd android
./gradlew clean
cd ..
flutter pub get
flutter build apk --release
```

---

## ✅ النتيجة المتوقعة

بعد اتباع هذه الخطوات:
- ✅ الإشعارات تعمل في APK Release
- ✅ FCM tokens يتم الحصول عليها بشكل صحيح
- ✅ الإشعارات تصل للمستخدمين والسائقين
- ✅ لا توجد أخطاء `messaging/registration-token-not-registered`

**🎉 الآن يمكنك بناء APK Release والإشعارات يجب أن تعمل!**

