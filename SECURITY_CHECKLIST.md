# ✅ قائمة فحص الأمان - تطبيق المنقذ

## قبل كل إصدار، تأكد من:

### 🔒 الأمان الأساسي
- [ ] لا توجد كلمات مرور في الكود المصدري
- [ ] لا توجد API keys مكشوفة في الكود
- [ ] `keystore.properties` موجود ومملوء بالقيم الصحيحة
- [ ] `keystore.properties` في `.gitignore` (لا يُرفع إلى Git)
- [ ] ملفات `.keystore` في `.gitignore`

### 🌐 الأمان الشبكي
- [ ] جميع الاتصالات تستخدم HTTPS فقط
- [ ] `network_security_config.xml` مفعل
- [ ] `usesCleartextTraffic="false"` في AndroidManifest
- [ ] لا توجد روابط HTTP في الكود

### 🛡️ حماية الكود
- [ ] ProGuard مفعل في release builds
- [ ] `minifyEnabled = true` في release
- [ ] `shrinkResources = true` في release
- [ ] ProGuard rules محدثة وصحيحة

### 📝 Logging
- [ ] لا توجد `print()` statements في production code
- [ ] جميع Logs تستخدم `AppLogger`
- [ ] Logs في release builds محدودة (فقط warnings و errors)

### 🔍 فحص الكود
- [ ] لا توجد معلومات حساسة في logs
- [ ] لا توجد معلومات debug في production
- [ ] جميع TODO comments تمت مراجعتها
- [ ] لا توجد كود مكرر غير ضروري

### 📦 البناء
- [ ] APK مبني في release mode
- [ ] APK مختبر على أجهزة حقيقية
- [ ] لا توجد أخطاء ProGuard
- [ ] حجم APK معقول (مقارنة بالإصدارات السابقة)

## 🚨 تحذيرات أمنية

1. **لا ترفع `keystore.properties` إلى Git** - يحتوي على كلمات مرور
2. **لا ترفع `.keystore` files** - ملفات التوقيع الحساسة
3. **راجع ProGuard rules** قبل كل إصدار
4. **اختبر APK** بعد كل تغيير في ProGuard
5. **استخدم HTTPS فقط** في production

## 📋 بعد كل إصدار

- [ ] تحديث changelog
- [ ] توثيق التغييرات الأمنية
- [ ] مراجعة logs في production
- [ ] مراقبة الأخطاء والتحذيرات





